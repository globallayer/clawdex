[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt",
    "pip install -e ."
]

[start]
cmd = "uvicorn clawdex.api.server:app --host 0.0.0.0 --port ${PORT:-8000}"
